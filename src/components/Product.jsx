import React from 'react';

import Typography from '@material-ui/core/Typography';
import { makeStyles } from '@material-ui/core/styles';

const Product = (props) => {
    console.log(props)

    
    return (
        <div>
            <Typography variant={"h3"}>Product Page</Typography>
        </div>
    )
}

export default Product